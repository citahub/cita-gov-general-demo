{
  "name": "gov-contracts",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/cryptape/appchain-gov-concil",
  "author": "Keith <keithwhisper@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "nodemon ./deploy.js",
    "compile": "cd ./contracts && solcjs PermsMng.sol Entry.sol --abi && solcjs PermsMng.sol Entry.sol --bin",
    "deploy": "yarn run compile && node ./deploy.js",
    "test": "jest"
  },
  "dependencies": {
    "@appchain/base": "^2.0.0"
  },
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
